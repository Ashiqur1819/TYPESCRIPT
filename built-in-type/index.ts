// Built in data type: number, string, boolean, null, undefined, void etc...

let userId: number;
let userName: string;
let age: number;
let isMarried: boolean;

userId = 101;
userName = "Nusrat Jahan";
(age = 18), (isMarried = true);

console.log({ userId, userName, age, isMarried });
